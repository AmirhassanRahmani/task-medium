import circle from "../images/image 4.jpg";
import save from "../images/save.jpg";

const listImages = [
  `${circle}`,
  `${circle}`,
  `${circle}`,
  `${circle}`,
  `${circle}`,
  `${circle}`,
  `${circle}`,
  `${save}`,
];

export default listImages;
